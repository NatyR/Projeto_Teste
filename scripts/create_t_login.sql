-- PORTALRH.T_LOGIN definition

-- DDL generated by DBeaver
-- WARNING: It may differ from actual native database DDL
CREATE TABLE PORTALRH.T_LOGIN (
	ID NUMBER NOT NULL,
	USUARIO_ID NUMBER NULL,
	EMAIL VARCHAR2(100) NOT NULL,
	IP VARCHAR2(50) NOT NULL,
	USERAGENT VARCHAR2(200) NULL,
	SITUACAO VARCHAR2(50) NULL,
	DATA_CADASTRO DATE NOT NULL,
	CONSTRAINT T_LOGIN_PK PRIMARY KEY (ID),
	CONSTRAINT FK_T_USUARIO FOREIGN KEY (USUARIO_ID) REFERENCES PORTALRH.T_USUARIO(ID)
);